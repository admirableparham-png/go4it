# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# Local data & secrets (never commit these)
data.db
data.db-journal
data.db-wal
data.db-shm
.env
.env.*
!.env.example

# Keys / credentials / editor backups (defense against an accidental `git add -A`)
*.pem
*.key
*.p12
*.pfx
credentials.json
*.swp
*~

# Ingestion inbox (buyer-lead data — never commit)
inbox/*.csv
inbox/processed/

# Scraper debug dumps (page HTML/screenshots may contain buyer data)
debug/
# Saved go4worldbusiness session cookies (sensitive)
.go4world_session.json

# OS / editor
.DS_Store
.idea/
.vscode/

# debug server access log (transient)
capture_access.log
